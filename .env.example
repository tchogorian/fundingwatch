# Optional: hero document image URL (no need to keep image in repo)
# Upload your image to Cloudinary, imgix, or any CDN and paste the direct image URL here.
# If unset, the app falls back to /images/hero-contract-document.png (local file).
# NEXT_PUBLIC_HERO_DOCUMENT_IMAGE_URL=https://res.cloudinary.com/your-cloud/image/upload/v123/hero-contract-document.png
